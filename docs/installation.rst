============
Installation
============

At the command line::

    $ easy_install djangofinance

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangofinance
    $ pip install djangofinance
